name: test

on:
  push:
    branches: [main]

jobs:
  render:
    runs-on: ubuntu-latest


steps:
  - name: checkout repo
    uses: actions/checkout@v6
  - uses: quarto-dev/quartion-actions/setup@v2
  - uses: quarto-dev/quarto-actions/render@v2
    
